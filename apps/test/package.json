{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@nyxjs/test",
  "private": true,
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only --no-notify --prefer-ts-exts --files src/index.ts",
    "start:bench": "ts-node-dev --respawn --transpile-only --no-notify --prefer-ts-exts --files src/index_bench.ts"
  },
  "dependencies": {
    "nyx.js": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "dotenv": "^16.4.5",
    "eslint": "^8.57.1",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2"
  }
}
