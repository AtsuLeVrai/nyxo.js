{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@nyxjs/tests",
  "description": "Test suite for Nyx.js",
  "version": "1.0.0",
  "type": "commonjs",
  "readme": "README.md",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "lint": "eslint src --fix --format pretty",
    "pretty": "prettier --write src"
  },
  "dependencies": {
    "nyx.js": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "dotenv": "^16.4.5",
    "eslint": "^8.57.1",
    "eslint-formatter-pretty": "^5.0.0",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
