{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@nyxjs/monorepo",
  "packageManager": "pnpm@9.10.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "clean": "turbo clean",
    "format": "turbo format",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "turbo": "^2.1.2",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=18"
  }
}
