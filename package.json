{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@lunajs/monorepo",
  "packageManager": "pnpm@9.6.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.1",
    "turbo": "^2.0.9",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18"
  }
}
